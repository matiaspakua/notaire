schema: notaire-sdlc
created: 2026-09-13
# Pure refactor: the REST contract, the persisted data and the DB schema are all
# unchanged, so there is no behavior delta to specify. The existing suites act as
# characterization tests instead — see design.md, Regression Strategy.
skip_specs: true
