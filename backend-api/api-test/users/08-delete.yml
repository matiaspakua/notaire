info:
  name: Delete user
  type: http
  seq: 8
  description: |-
      ## Delete user — DELETE /api/v1/usuarios/{api_user_id}
      Traceability: CU21 (RF #43, #47, #82, #83)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/usuarios/{{api_user_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 204 No Content", function () { expect(res.getStatus()).to.equal(204); });
