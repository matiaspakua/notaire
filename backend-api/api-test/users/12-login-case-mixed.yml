info:
  name: Login with mixed-case username (case-insensitive)
  type: http
  seq: 12
  description: |-
      ## Login with mixed-case username (case-insensitive) — POST /api/v1/usuarios/login
      Traceability: CU78 (RF #81, #82, #83, #84, #85)
http:
  method: POST
  url: "{{base_url}}/api/v1/usuarios/login"
  body:
    type: json
    data: |-
        {"name": "{{case_user_name}}", "password": "Test1234!"}
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () { expect(res.getStatus()).to.equal(200); });
          test("Mixed-case username logs in", function () { expect(res.getBody().valido).to.equal(true); });
