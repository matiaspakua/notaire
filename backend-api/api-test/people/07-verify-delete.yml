info:
  name: Verify person deleted
  type: http
  seq: 7
  description: |-
      ## Verify person deleted — GET /api/v1/people/{{person_id}}
      Traceability: CU46 (RF #38, #41)
http:
  method: GET
  url: "{{base_url}}/api/v1/people/{{person_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
