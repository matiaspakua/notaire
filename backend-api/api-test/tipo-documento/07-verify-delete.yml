info:
  name: Verify Tipo de Documento Deleted
  type: http
  seq: 7
http:
  method: GET
  url: "{{base_url}}/api/v1/tipo-de-documento/{{_id}}"
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
