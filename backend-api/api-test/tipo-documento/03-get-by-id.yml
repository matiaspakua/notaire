info:
  name: Get Tipo de Documento By ID
  type: http
  seq: 3
http:
  method: GET
  url: "{{base_url}}/api/v1/tipo-de-documento/{{_id}}"
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () { expect(res.getStatus()).to.equal(200); });
          test("Returns the requested item", function () {
            expect(res.getBody().idTipoDocumento).to.equal(Number(bru.getVar('_id')));
          });
