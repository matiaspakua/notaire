info:
  name: Teardown - Delete notary person
  type: http
  seq: 12
  description: |-
      ## Teardown — DELETE /api/v1/people/{id}
      Traceability: CU48 (RF #43, #113, #114)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/people/{{history_notary_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 204 No Content", function () { expect(res.getStatus()).to.equal(204); });
