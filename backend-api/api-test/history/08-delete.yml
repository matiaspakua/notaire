info:
  name: Delete history entry
  type: http
  seq: 9
  description: |-
      ## Delete history entry — DELETE /api/v1/historial/{id}
      Traceability: CU13 (RF #3, #24, #26, #110)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/historial/{{history_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
