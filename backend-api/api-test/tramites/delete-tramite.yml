info:
  name: Delete Tramite
  type: http
  description: |-
      ## Delete Tramite
      Elimina un trámite del sistema.
      
      **Method:** DELETE
      **Path:** /api/v1/tramites/{id}

http:
  method: DELETE
  url: "{{base_url}}/api/v1/tramites/1"

runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () {
            expect(res.getStatus()).to.equal(200);
          });

