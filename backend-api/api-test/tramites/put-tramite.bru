meta {
  name: Update Tramite
  type: http
}

put {
  url: {{base_url}}/api/v1/tramites/1
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  nombre: "Trámite actualizado"
  descripcion: "Descripción actualizada"
}

tests {
  test("Status 200", function () {
    expect(res.getStatus()).to.equal(200);});
}
docs {
  ## Update Tramite
  Actualiza un trámite existente en el sistema.
  **Method:** PUT
  **Path:** /api/v1/tramites/{id}
}
