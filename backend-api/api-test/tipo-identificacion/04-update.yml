info:
  name: Update Tipo de Identificacion
  type: http
  seq: 4
http:
  method: PUT
  url: "{{base_url}}/api/v1/tipo-identificacion/{{_id}}"
  body:
    type: json
    data: |-
        {"nombre":"TI E2E Actualizado","caracteres":"11"}
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 or 204", function () { expect([200,204]).to.include(res.getStatus()); });
