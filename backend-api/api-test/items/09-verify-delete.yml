info:
  name: Verify item deleted
  type: http
  seq: 10
  description: |-
      ## Verify item deleted — GET /api/v1/items/{item_id}
      Traceability: CU71, CU45 (RF #4, #7, #8, #9)
http:
  method: GET
  url: "{{base_url}}/api/v1/items/{{item_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
