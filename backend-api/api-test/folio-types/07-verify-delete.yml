info:
  name: Verify folio type deleted
  type: http
  seq: 7
  description: |-
      Traceability: CU58 (RF #55, #94, #119)
http:
  method: GET
  url: "{{base_url}}/api/v1/tipo-folio/{{folio_type_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
