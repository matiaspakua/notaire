info:
  name: List substitutions
  type: http
  seq: 4
  description: |-
      ## List substitutions — GET /api/v1/suplencia
      Traceability: CU59 (RF #43, #113, #116)
http:
  method: GET
  url: "{{base_url}}/api/v1/suplencia"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
          test("Returns an array", function () { expect(res.getBody()).to.be.an('array'); });
