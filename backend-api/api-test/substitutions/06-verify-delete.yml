info:
  name: Verify substitution deleted
  type: http
  seq: 8
  description: |-
      ## Verify substitution deleted — GET /api/v1/suplencia/{{substitution_id}}
      Traceability: CU59 (RF #43, #113, #116)
http:
  method: GET
  url: "{{base_url}}/api/v1/suplencia/{{substitution_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 Not Found", function () { expect(res.getStatus()).to.equal(404); });
