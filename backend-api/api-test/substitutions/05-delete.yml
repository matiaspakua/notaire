info:
  name: Delete substitution
  type: http
  seq: 7
  description: |-
      ## Delete substitution — DELETE /api/v1/suplencia/{{substitution_id}}
      Traceability: CU22 (RF #43, #113, #114, #115)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/suplencia/{{substitution_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 or 204", function () { expect([200,204]).to.include(res.getStatus()); });
