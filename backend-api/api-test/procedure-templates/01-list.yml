info:
  name: List procedure templates
  type: http
  seq: 1
  description: |-
      ## List procedure templates — GET /api/v1/plantilla-tramite
      Traceability: CU79 (RF #61, #62, #63, #64, #65)
http:
  method: GET
  url: "{{base_url}}/api/v1/plantilla-tramite"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
          test("Returns an array", function () { expect(res.getBody()).to.be.an('array'); });
