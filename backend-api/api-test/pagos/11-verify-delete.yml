info:
  name: Verify Pago Delete
  type: http
  seq: 12
  description: "## GET /api/v1/pagos/{id} — debe devolver 404 tras el delete"
http:
  method: GET
  url: "{{base_url}}/api/v1/pagos/{{pago_id}}"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 Not Found", function () {
            expect(res.getStatus()).to.equal(404);
          });
