info:
  name: List Pagos
  type: http
  seq: 3
  description: "## GET /api/v1/pagos"
http:
  method: GET
  url: "{{base_url}}/api/v1/pagos"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () {
            expect(res.getStatus()).to.equal(200);
          });
          test("Includes created pago", function () {
            const b = res.getBody();
            expect(b.some(p => p.idPago === bru.getVar('pago_id'))).to.be.true;
          });
