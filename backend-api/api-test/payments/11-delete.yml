info:
  name: Delete payment
  type: http
  seq: 12
  description: |-
      ## Delete payment — DELETE /api/v1/pagos/{payment_id}
      Traceability: CU15 (RF #3, #20, #21, #22, #23)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/pagos/{{payment_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
