info:
  name: Teardown - Delete budget
  type: http
  seq: 14
  description: |-
      ## Teardown - Delete budget — DELETE /api/v1/presupuestos/{payments_budget_id}
      Traceability: CU45 (RF #4, #7, #8, #9)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/presupuestos/{{payments_budget_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 204 No Content", function () { expect(res.getStatus()).to.equal(204); });
