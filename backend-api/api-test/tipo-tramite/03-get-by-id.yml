info:
  name: Get Tipo de Tramite By ID
  type: http
  seq: 3
http:
  method: GET
  url: "{{base_url}}/api/v1/tipo-tramite/{{_id}}"
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () { expect(res.getStatus()).to.equal(200); });
          test("Returns the requested item", function () {
            expect(res.getBody().idTipoDeTramite).to.equal(Number(bru.getVar('_id')));
          });
