info:
  name: Verify budget template deleted
  type: http
  seq: 8
  description: |-
      ## Verify budget template deleted — GET /api/v1/plantilla-presupuestos/tipo-tramite/{template_procedure_type_id}
      Traceability: CU49 (RF #6, #61, #66, #69)
http:
  method: GET
  url: "{{base_url}}/api/v1/plantilla-presupuestos/tipo-tramite/{{template_procedure_type_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
          test("Template list for the procedure type is empty", function () { expect(res.getBody()).to.have.lengthOf(0); });
