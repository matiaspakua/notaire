info:
  name: Delete budget template
  type: http
  seq: 7
  description: |-
      ## Delete budget template — DELETE /api/v1/plantilla-presupuestos/tipo-tramite/{template_procedure_type_id}/concepto/{template_concept_id}
      Traceability: CU49 (RF #6, #61, #66, #69)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/plantilla-presupuestos/tipo-tramite/{{template_procedure_type_id}}/concepto/{{template_concept_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
