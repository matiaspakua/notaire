info:
  name: Update budget template
  type: http
  seq: 6
  description: |-
      ## Update budget template — PUT /api/v1/plantilla-presupuestos/tipo-tramite/{template_procedure_type_id}/concepto/{template_concept_id}
      Traceability: CU55 (RF #6, #61, #66, #68)
http:
  method: PUT
  url: "{{base_url}}/api/v1/plantilla-presupuestos/tipo-tramite/{{template_procedure_type_id}}/concepto/{{template_concept_id}}"
  body:
    type: json
    data: |-
        {"notes":"E2E budget template updated"}
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
