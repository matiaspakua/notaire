info:
  name: Teardown - Delete concept
  type: http
  seq: 9
  description: |-
      ## Teardown - Delete concept — DELETE /api/v1/conceptos/{template_concept_id}
      Traceability: CU37 (RF #55, #60)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/conceptos/{{template_concept_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 204 No Content", function () { expect(res.getStatus()).to.equal(204); });
