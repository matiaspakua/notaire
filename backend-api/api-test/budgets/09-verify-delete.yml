info:
  name: Verify budget deleted
  type: http
  seq: 9
  description: |-
      ## Verify budget deleted — GET /api/v1/presupuestos/{budget_id}
      Traceability: CU60 (RF #4, #5, #7)
http:
  method: GET
  url: "{{base_url}}/api/v1/presupuestos/{{budget_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
