info:
  name: Delete procedure
  type: http
  seq: 6
  description: |-
      ## Delete procedure — DELETE /api/v1/tramites/{procedure_id}
      Traceability: CU53 (RF #3, #13)
http:
  method: DELETE
  url: "{{base_url}}/api/v1/tramites/{{procedure_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () { expect(res.getStatus()).to.equal(200); });
