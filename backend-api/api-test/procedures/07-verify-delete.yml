info:
  name: Verify procedure deleted
  type: http
  seq: 7
  description: |-
      ## Verify procedure deleted — GET /api/v1/tramites/{procedure_id}
      Traceability: CU53 (RF #3, #13)
http:
  method: GET
  url: "{{base_url}}/api/v1/tramites/{{procedure_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
