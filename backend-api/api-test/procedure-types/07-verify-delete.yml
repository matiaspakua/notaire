info:
  name: Verify procedure type deleted
  type: http
  seq: 7
  description: |-
      Traceability: CU57 (RF #55, #56)
http:
  method: GET
  url: "{{base_url}}/api/v1/tipo-tramite/{{procedure_type_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
