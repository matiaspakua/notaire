info:
  name: Delete Historial
  type: http
  seq: 9
  description: |-
      ## DELETE /api/v1/historial/{id}
http:
  method: DELETE
  url: "{{base_url}}/api/v1/historial/{{_historial_id}}"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 OK", function () {
            expect(res.getStatus()).to.equal(200);
          });
