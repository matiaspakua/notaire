info:
  name: Setup - Create Escribano Persona
  type: http
  seq: 1
  description: |-
      ## Setup — POST /api/v1/people
      Crea la persona escribano requerida por el alta de gestion, para que la
      suite sea autosuficiente en una base de datos recién migrada.
http:
  method: POST
  url: "{{base_url}}/api/v1/people"
  body:
    type: json
    data: |-
        {
          "firstName": "Escribano",
          "lastName": "Historial E2E",
          "identificationNumber": "30555447",
          "isClient": false,
          "fkIdIdentificationType": { "idTipoIdentificacion": 1 },
          "nationality": "Argentina",
          "maritalStatus": "Soltero",
          "sex": "Masculino"
        }
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 201 Created or 409 Conflict", function () {
            expect([200, 201, 409]).to.include(res.getStatus());
          });
          test("Stores escribano id", function () {
            const b = res.getBody();
            const id = b.personId || b.idPersonaExistente;
            expect(id).to.be.a('number');
            bru.setVar('_historial_escribano_id', id);
          });
