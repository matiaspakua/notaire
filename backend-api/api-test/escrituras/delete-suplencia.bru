meta {
  name: Delete Suplencia
  type: http
}

delete {
  url: {{base_url}}/api/v1/suplencia/{{suplencia_id}}
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200 - Suplencia deleted", function () {
      expect(res.getStatus()).to.equal(200);
    });
}
docs {
  ## Delete Suplencia
  Elimina una suplencia existente.

  **Method:** DELETE
  **Path:** /api/v1/suplencia/{id}
}