info:
  name: Update identification type
  type: http
  seq: 4
  description: |-
      Traceability: CU17 (RF #12, #38, #39)
http:
  method: PUT
  url: "{{base_url}}/api/v1/tipo-identificacion/{{identification_type_id}}"
  body:
    type: json
    data: |-
        {"name":"E2E Identification Type Updated","characters":"11"}
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 or 204", function () { expect([200,204]).to.include(res.getStatus()); });
