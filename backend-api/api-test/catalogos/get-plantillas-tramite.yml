info:
  name: Get All Plantillas Tramite
  type: http
  description: |-
      ## Get All Plantillas Tramite
      Obtiene todas las plantillas de trámite.
      
      **Method:** GET
      **Path:** /api/v1/plantilla-tramite

http:
  method: GET
  url: "{{base_url}}/api/v1/plantilla-tramite"
  headers:
    Content-Type: application/json

runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () {
            expect(res.getStatus()).to.equal(200);
          });
           
          test("Response is array", function () {
            const body = res.getBody();
            expect(body).to.be.an('array');
          });

