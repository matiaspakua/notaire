meta {
  name: Delete Folio
  type: http
}

delete {
  url: {{base_url}}/api/v1/folio/{{folio_id}}
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200 - Folio deleted", function () {
      expect(res.getStatus()).to.equal(200);
    });
}
docs {
  ## Delete Folio
  Elimina un folio existente.

  **Method:** DELETE
  **Path:** /api/v1/folio/{id}
}