meta {
  name: Delete Tipo Documento
  type: http
}

delete {
  url: {{base_url}}/api/v1/tipo-de-documento/1
  body: none
  auth: none
}

tests {
  test("Status 200", function () {
    expect(res.getStatus()).to.equal(200);});
}
docs {
  ## Delete Tipo Documento
  Elimina un tipo de documento del sistema.
  **Method:** DELETE
  **Path:** /api/v1/tipo-de-documento/{id}
}
