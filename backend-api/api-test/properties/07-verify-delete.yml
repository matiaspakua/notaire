info:
  name: Verify property deleted
  type: http
  seq: 7
  description: |-
      ## Verify property deleted — GET /api/v1/inmueble/{property_id}
      Traceability: CU69 (RF #117)
http:
  method: GET
  url: "{{base_url}}/api/v1/inmueble/{{property_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
