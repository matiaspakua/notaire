info:
  name: Verify folio deleted
  type: http
  seq: 6
  description: |-
      ## Confirm deletion — GET deleted id returns 404
      Traceability: CU63 (RF #55, #58, #94, #95, #96, #99)
http:
  method: GET
  url: "{{base_url}}/api/v1/folio/{{folio_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
