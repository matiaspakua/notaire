info:
  name: Get Tipo de Folio By ID
  type: http
  seq: 3
http:
  method: GET
  url: "{{base_url}}/api/v1/tipo-folio/{{_id}}"
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () { expect(res.getStatus()).to.equal(200); });
          test("Returns the requested item", function () {
            expect(res.getBody().idTipoFolio).to.equal(Number(bru.getVar('_id')));
          });
