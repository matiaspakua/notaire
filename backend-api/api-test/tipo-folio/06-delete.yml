info:
  name: Delete Tipo de Folio
  type: http
  seq: 6
http:
  method: DELETE
  url: "{{base_url}}/api/v1/tipo-folio/{{_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 or 204", function () { expect([200,204]).to.include(res.getStatus()); });
