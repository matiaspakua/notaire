info:
  name: Get Estado de Gestion By ID
  type: http
  seq: 3
http:
  method: GET
  url: "{{base_url}}/api/v1/estado-gestion/{{_id}}"
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200", function () { expect(res.getStatus()).to.equal(200); });
          test("Returns the requested item", function () {
            expect(res.getBody().idEstadoGestion).to.equal(Number(bru.getVar('_id')));
          });
