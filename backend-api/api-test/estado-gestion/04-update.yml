info:
  name: Update Estado de Gestion
  type: http
  seq: 4
http:
  method: PUT
  url: "{{base_url}}/api/v1/estado-gestion/{{_id}}"
  body:
    type: json
    data: |-
        {"nombre":"EG E2E Actualizado","observaciones":"upd"}
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 200 or 204", function () { expect([200,204]).to.include(res.getStatus()); });
