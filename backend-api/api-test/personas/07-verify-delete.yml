info:
  name: Verify Persona Deleted
  type: http
  seq: 7
http:
  method: GET
  url: "{{base_url}}/api/v1/personas/{{persona_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{token}}"
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
