info:
  name: Verify Presupuesto Deleted
  type: http
  seq: 8
http:
  method: GET
  url: "{{base_url}}/api/v1/presupuestos/{{presupuesto_id}}"
  headers:
    Content-Type: application/json
runtime:
  scripts:
    - type: tests
      code: |-
          test("Status 404 after delete", function () { expect(res.getStatus()).to.equal(404); });
