meta {
  name: Delete Presupuesto
  type: http
}

delete {
  url: {{base_url}}/api/v1/presupuestos/{{presupuesto_id}}
  body: none
  auth: none
}

tests {
  test("Status 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  ## Delete Presupuesto
  Elimina un presupuesto del sistema.
  
  **Method:** DELETE
  **Path:** /api/v1/presupuestos/{id}
}
