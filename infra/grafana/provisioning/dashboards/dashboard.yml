apiVersion: 1

providers:
  - name: 'Notaire Dashboards'
    orgId: 1
    folder: 'Notaire'
    folderUid: 'notaire'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
